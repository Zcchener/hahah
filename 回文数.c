#include<stdio.h>
#include<malloc.h>
typedef struct stacknode
{
	int data[N];
	
}stack,
 push()
 {
 	
 	
 }
 pop
 {
 	
 	
 }
 Judgement
 {
 	length=strlen(str); 
 	for(i=0;i<length/2;i++)//前二分之一元素进栈 
 	push( );
 	if(length%2==0)
 	{
 		//下一个元素和栈顶元素比较，相同，栈顶元素出栈 
	 }
	 if(length%2==1)
	 {
	 	
	 	//1，栈顶元素出栈 
	 	//2.下一个元素和栈顶元素比较，相同，栈顶元素出栈 
	  } 
 	
 	
 	
 }
 main()
 {
 	
 	
 	
 	
 }
